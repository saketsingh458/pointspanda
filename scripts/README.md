# Scripts

## validate-cards.mjs

Validates `data/cards.json` against structural rules (required fields, category multipliers, no duplicate ids).

```bash
npm run validate:cards
```

Run after editing `data/cards.json` to ensure the catalog is valid before deploy.
